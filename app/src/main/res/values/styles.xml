<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="tabThing" parent="AppTheme">
        <item name="android:textSize">5sp</item>
    </style>

</resources>
